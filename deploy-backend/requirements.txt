# ============================================
# TELECOM AI ASSISTANT - GPU REQUIREMENTS
# ============================================
# NOTE: torch and torchaudio are installed separately
# in the Dockerfile with CUDA support using:
#   pip install torch==2.5.1+cu121 torchaudio==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121

# FastAPI and ASGI Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
websockets==12.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Redis
redis==5.0.1
hiredis==2.3.2

# Pydantic
pydantic==2.5.3
pydantic-settings==2.1.0

# LLM and Embeddings
ollama==0.1.6
sentence-transformers==2.3.1
chromadb==0.4.22
rank-bm25==0.2.2

# Voice Processing - GPU Accelerated
# NOTE: torch/torchaudio installed separately with CUDA in Dockerfile
faster-whisper==1.2.1
silero-vad==5.1.2
numpy==1.26.3
kokoro>=0.9.2
soundfile

# Spacy - must be 3.8+ for Python 3.12 compatibility
spacy>=3.8.0

# CTranslate2 for faster-whisper GPU support
ctranslate2>=4.0.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# HTTP Client
httpx==0.25.2
aiohttp==3.9.1

# Utilities
python-dateutil==2.8.2
pytz==2023.3

# Logging
structlog==24.1.0

# ============================================
# REMOVED FROM THIS FILE (installed in Dockerfile):
# - torch (needs CUDA version)
# - torchaudio (needs CUDA version)
#
# REMOVED (not needed for production):
# - pytest, pytest-asyncio, pytest-cov (testing)
# - black, flake8, mypy, isort (code quality)
# ============================================
